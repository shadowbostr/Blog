<%= form_with(model: topic) do |form| %>
  <% if topic.errors.any? %>
    <div id="error_explanation" class="alert alert-danger alert-dismissible">
      <h6><%= pluralize(topic.errors.count, "error") %> prohibited this topic from being saved:</h6>

      <ul>
        <% topic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
